<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestDemo">
        <activity android:name=".Home"></activity>
        <activity android:name=".sign_main_app" />
        <activity android:name=".profile_app" />
        <activity android:name=".forgotpass_app" />
        <activity android:name=".chat_app" />
        <activity android:name=".add_target_app" />
        <activity android:name=".achievenment_app" />
        <activity
            android:name=".MainActivity"
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>